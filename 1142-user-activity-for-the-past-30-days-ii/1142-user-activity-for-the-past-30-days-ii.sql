select ifnull(round(sum(a)/count(user_id),2),0) as average_sessions_per_user from (select user_id,count(distinct session_id) as a from activity where datediff('2019-07-27',activity_date)<30 group by user_id) m